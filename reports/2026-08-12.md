# AI Security Radar — Daily Report 2026-08-12 (Wednesday)

FULL CHECK of every mandatory lane (last daily 08-11, weekly W32 08-08). The
substance came from the fresh cs.CR batch (window 2608.09914→2608.11187, ~30 new
ids, 6 abstracts opened): one cap rotation on **adversarial-trigger-backdoor-004**
plus a backdoor/agent-web/watermark queue. Primary feeds, standards, repo-watch and
pulse were quiet (no new on-axis primary since 08-11).

## State
- No stranded state. A mid-run stale-ref scare (my combined `git fetch origin main
  claude/…` aborted on the missing `claude/…` remote ref before updating `origin/main`,
  leaving the local ref at 4c6138c/08-10) suggested the 08-11 commit had not reached
  `main` — but the actual push resolved `542c205..a5e4e64`, confirming remote `main` was
  already at the 08-11 commit (542c205). Today's `git push origin HEAD:main` was a clean
  fast-forward; `main` is current. Lesson: fetch refs separately so one missing ref does
  not abort the whole fetch and strand a stale `origin/main`.

## Ledger changes
- **adversarial-trigger-backdoor-004 +1 via cap ROTATION** (no stage move; held
  accelerating/high). Rotated IN [Once Poisoned, Arbitrarily Controlled: A Programmable
  Backdoor in VLMs](https://arxiv.org/abs/2608.10959) (2608.10959, 08-11) — a single
  poisoning phase implants an any-to-any PROGRAMMABLE backdoor: the attacker picks
  previously-unseen target-caption semantics at inference time and synthesizes a matching
  stealthy trigger on demand (target selection decoupled from poisoning), surviving several
  classical defenses. A qualitatively stronger threat model than every prior 004 item (all
  bind triggers to a fixed target set before training). Rotated OUT the oldest/narrowest line,
  "Color Matters" (2606.25858, 06-24) — its federated-learning coverage is retained by "When
  the Aggregator Cheats". last_evidence 07-28→08-11 (pulls 004 back from a 14-day quiet drift).
- **No other stage move.** Today's other on-axis primaries were queued as rotate-candidates.
- **Queue:** +1 08-12 line (5 captures); burndown dropped the 27-day 2026-07-16 Embrace The
  Red macOS-Terminal DNS-exfil item (001's indirect-PI→exfil facet amply carried). Net
  watchlist ~24, under the ~25 soft cap.

## Top 3 of the day
1. **[Programmable Backdoor in VLMs](https://arxiv.org/abs/2608.10959)** — decouples backdoor
   target selection from poisoning: one poisoning pass, then any-to-any caption control with
   on-demand stealthy triggers. → 004.
2. **[MarkNull](https://arxiv.org/abs/2608.10166)** (USENIX Security 2026) — model-agnostic
   watermark removal via on-manifold latent decorrelation drops bit-accuracy to ~53% and
   defeats Google SynthID-Image with no visible degradation; transfers to video. → study pick +
   provenance-attack nucleus to watch.
3. **[Trigger the Straggler / Load Hijack on MoE LLMs](https://arxiv.org/abs/2608.10614)** — a
   poisoned router (checkpoint-only tamper + private trigger) concentrates load on one GPU on
   trigger, stalling peers (1.43× TTFT) — an availability/serving-schedule supply-chain backdoor.
   → queue; forms a DoS-on-inference convergence with the persona-conditioning infinite-thinking
   attack (2 groups).

## Study picks
- [MarkNull: Model-Agnostic Watermark Removal in AI-Generated Images](https://arxiv.org/abs/2608.10166)
  — the clean USENIX-2026 anchor for why AI-image provenance/watermarking is not yet a reliable
  integrity control: latent decorrelation removes the mark near-losslessly and defeats a deployed
  system (SynthID-Image).

## Coverage / integrity
- **capture-leak: 6 new arXiv ids checked / 5 queued + 1 evidence, 0 leaked** — 2608.10959 →
  004 evidence + queue; 10614 / 10281 / 10530 / 10171 / 10166 → the 08-12 queue line (10166 also
  study_shelf); grep confirmed all land in TRENDS.md, none orphaned in prose.
- **Convergence / new-axis check**: two sub-nuclei surfaced but neither clears ≥3 groups —
  (a) availability/DoS-on-LLM-inference (Load Hijack MoE + persona-conditioning infinite-thinking,
  2 groups); (b) provenance/watermark-removal attacks (MarkNull, 1 group; hackerfactor's
  Un-Stable-Signature is a staged-source candidate, not queued evidence). Watch both.
- **Repo-watch**: garak 0.16.0 / pyrit 1.0.1 / deepteam 1.0.8 / giskard 2.19.2 /
  promptfoo 0.122.0 — ALL unchanged since 08-07.
- **Advisory**: NVD "MCP server" 08-11→08-12 = 2 CVEs — CVE-2026-19516 (mcp-grafana SSRF) NEW
  distinct → 001 MCP-CVE burst; CVE-2026-72768 (n8n SSRF-bypass) not AI-specific.
- **Tool-discovery**: registry SEARCH surfaced **probeagent-ai** (github.com/sumamovva/probeagent,
  PyPI v0.3.6 07-25, automated PI/jailbreak red-team CLI) → staged as a discovered tool/source
  candidate (early-stage, below study-shelf bar). GitHub topic search = awesome-lists only.
- **Pulse**: no offensive-AI earthquake; "Azure DevOps MCP Flaw Lets Hidden PR Comments Hijack AI
  Review Agents" (thehackernews.com) queued as 001-axis intake (unverified, news).
- **Degradations**: none new.

## Next
- Give **004** its next rotation slot to Load Hijack (MoE availability backdoor) or DFCS if the
  backdoor cadence continues; watch whether the availability/DoS-on-inference nucleus reaches a 3rd
  group.
- Give **001** a rotation slot to LLM2X (LLM-mediated classic-web-vuln) if the confused-deputy
  cadence keeps landing; verify the Azure DevOps MCP comment-hijack against a primary artifact.
- Watch for a 2nd watermark/provenance-removal group before seeding that axis.
- Re-check the DEF CON 34 flagships (PromptPwn, Zealot, X-Ray Your Agents) for public repos.
