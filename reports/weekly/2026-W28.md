# AI Security Radar — Weekly report 2026-W28
# Week of 2026-07-04 to 2026-07-11

One stage move (a genuine promotion, not a housekeeping call), two routing-queue
items finally resolved, and a source-coverage audit that found the same class
of multi-week miss W27 fixed for Anthropic/AI-Village — this time on OWASP/
MITRE-ATLAS and a confirmed-but-unused GTIG heal.

---

## Stage move

**adversarial-trigger-backdoor-004: emerging → accelerating.** Recalibrating
velocity since seeding (06-27): 8 independent attack groups have landed at a
sustained ~1-4-day clip across 6 model-type domains (diffusion, sensor/IoT,
federated learning, VLM-reasoning, speech, embodied/VLA-robotics) — squarely
"broad, fast." Confidence held medium: the standing caveat is domain
heterogeneity (no dominant domain, LLM/VLM sub-cluster still only 2 members),
a separate axis from the breadth/speed that drove the stage move.

## Routing fixes (this week's real finding)

- **Cross-Modal Membership Inference** (2607.04339) moved from the queue onto
  `model-extraction-fingerprinting-006`'s evidence, resolving an ambiguity the
  07-05 daily explicitly left open. 006's alias already spans extraction/
  distillation/fingerprinting and membership inference is named in the same
  scope axis, so a single primary suffices to append (last_evidence 07-01→07-05).
- **"Prompt Injection as Role Confusion"** (2603.12277) had sat below `001`'s
  cap for 6 CONSECUTIVE daily passes (07-05→07-10) while every fresher item won
  the rotation slot instead — a mechanically-detected routing-leak, fixed this
  session rather than deferred again. Rotated out ShareLock (06-25, preserved
  in git/reports; its evasion facet is now carried by the fresher Unicode
  TAG-Block evidence).

## Major source-coverage findings

1. **OWASP GenAI Top 10 / MITRE ATLAS** — daily logs wrote "not
   changed-checked this pass" for BOTH on every single day this week (7 for 7),
   repeating W27's exact flagged miss. Root cause matched the AI-Village
   pattern: both lived as plain bullets with no "swept every run" heading, so
   no daily lane actually owned them. Fixed by moving both into a new
   **Standards watch (swept every run)** heading in SOURCES.md.
2. **GTIG** — the tvly search+extract heal confirmed working on 07-04 sat
   UNUSED for 6 straight daily passes; each just re-logged the old
   `degraded: feed-not-itemized` label instead of running it. SOURCES.md
   corrected: the search+extract pipeline is now the required check.
3. **Protect AI / huntr** — actually opened `huntr.com/bounties/hacktivity`
   this session (candidate since 06-30, carried from W27) and found three
   dated ML-framework CVEs from the prior week alone, incl. a Keras
   Lambda-layer deserialization RCE — a live, itemized feed that had sat
   un-swept for 11 days after its access method was confirmed. **Promoted
   candidate→swept.**
4. **GitHub watch** — 7 of 12 watched repos (llm-attacks, ai-exploits,
   PurpleLlama, agentdojo, cybench, intercode, L1B3RT4S) have zero release
   coverage since the proxy scope-block began (~07-02): they have no
   PyPI/npm equivalent, and `tvly extract` on a commits page returned a
   STALE cache (newest shown commit was 5 months old) — not adopted as a
   heal. Genuine unresolved gap, carried to next week. Applied **W27
   Proposal F**: PyPI/npm promoted from fallback to PRIMARY method for the
   5 tools it does cover (garak, PyRIT, deepteam, giskard, promptfoo).
5. **OpenAI RSS** — empty 3 consecutive passes (07-08/09/10) — escalated to
   heal-watch (same threshold Project Zero crossed).

## Queue

24 items (soft cap ~25). This week: +17 added / 2 promoted to evidence (MIA,
role-confusion) / 16 dropped (cap-driven burndown, reasons in daily reports) /
2 re-verified past the 14-day mark (Alibaba-distillation claim — still no
Anthropic/Alibaba primary after a 3rd check; RIPA — still a 2-group embodied
nucleus, not dropped). Capture-leak mechanical sweep: 95 ids checked, 0 leaks.

## Source strategy
- Coverage: 21/23 swept-every-run sources opened-or-degraded (miss 2:
  OWASP/ATLAS, fixed at the root above; degr ~7).
- Source discovery: 1 new candidate staged (blog.zksecurity.xyz, 07-08, still
  1 appearance); 1 promoted via the separate manual-verification lane (Protect
  AI/huntr). Meta and Bishop Fox re-attempted, both still unresolved.
- Anchoring: no — this week's evidence/moves span 001, 002, 003, 004, 005 and
  006; not concentrated on one axis.

## Calibration (W28) — full line in `logs/calibration.md`
queue +17/→2p/−16d/stale 2 · evidence +11 (001+4, 002+1, 003+1, 004+1, 005+3,
006+1) · moves 2 (005 07-04, 004 07-11) · exploration 7/7 (100%) · off-axis
4/17 (~24%) · lag: median 2d (range 1-6d), backfill share 3/11 (~27%) ·
coverage 21/23 (miss 2, degr ~7) · routing-leak 1 found+fixed · capture-leak
95/0 · source-discovery 1 staged / 1 promoted (separate lane).

No monthly retrospective this week (not the first weekly of the month).

## Self-amendment
**Applied** (cooling period elapsed, both signals persisted from W27):
- **Proposal D** → `radar-self-eval` coverage metric now must check whether a
  CONFIRMED heal is actually being run, not just re-logged as degraded
  (motivated by GTIG sitting unused 6 days despite a documented working fix).
- **Proposal E** → `routines/weekly.md` now includes a rolling multi-week
  cross-check of SOURCES.md `[verified]` tags against cumulative coverage
  (motivated by OWASP/ATLAS repeating W27's exact miss).
- Proposal F (PyPI/npm to primary) applied as a SOURCES.md data correction,
  same treatment as the Anthropic/AI-Village fixes.

No rollback needed — no amendment showed two-week metric regression.

**New proposals for W29** (cooling period, apply next week if the signal
persists):
- **Proposal G**: if the 7 non-packaged watched GitHub repos remain
  uncoverable next week too, downgrade them from "swept every run" to a
  best-effort quarterly check rather than silently implying full coverage.
- **Proposal H**: the auto-staged "Discovered-source candidates" tally and the
  manually-tracked "candidate" lane under Primary feeds (Protect AI, Meta, NCC/
  Bishop Fox) are two separate review processes with different cadences —
  huntr's promotion this week came from the older manual lane, not the newer
  auto-stage mechanism. Consider merging them into one reviewed list.

## Next (priority order for W29)
1. Watch for a 2nd on-axis ML-framework/model-supply-chain CVE (cf. this
   week's Keras Lambda-RCE via huntr) to consider a dedicated nucleus.
2. Retry the 7 uncoverable GitHub repos with a fresh method before applying
   Proposal G's downgrade.
3. Heal OpenAI RSS (try `tvly extract` per the Project Zero pattern).
4. Meta / Bishop Fox extraction still unresolved — try a `--query`-scoped
   extract on Bishop Fox next.
5. Watch embodied-agent (RIPA + overthinking-slowdown) and
   hallucination-squatting (Phantom Squatting + HalluSquatting) nuclei — both
   still at 2 groups, need a 3rd to seed.
